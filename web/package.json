{
  "name": "quant-impl-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "start": "node ./dist/server/entry.mjs",
    "test": "node --test tests/predictions.test.mjs"
  },
  "dependencies": {
    "@astrojs/node": "^10.0.3",
    "astro": "^6.0.8"
  }
}
